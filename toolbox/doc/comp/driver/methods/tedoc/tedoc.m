clearvars; close all; clc;
%[text] # Method: `te`
%[text] JVsound, J.G. Vermond
%[text] ## Description
%[text] The method `te` resides in the class `comp.Driver`. It builds the electrical transmission (ABCD) matrix stack over frequency, embedding the blocked electrical impedance `zeb` into the matrix.

%[appendix]{"version":"1.0"}
%---
%[metadata:view]
%   data: {"layout":"inline"}
%---
