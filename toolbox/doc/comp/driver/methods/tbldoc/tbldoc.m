clearvars; close all; clc;
%[text] # Method: `tbl`
%[text] JVsound, J.G. Vermond
%[text] ## Description
%[text] The method `tbl` resides in the class `comp.Driver`. It builds the transduction (gyrator) transmission matrix that relates the driver's electrical and mechanical domains via its force factor `Bl`.

%[appendix]{"version":"1.0"}
%---
%[metadata:view]
%   data: {"layout":"inline"}
%---
