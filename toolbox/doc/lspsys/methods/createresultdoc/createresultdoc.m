clearvars; close all; clc;
%[text] # Method: `createResult`
%[text] JVsound, J.G. Vermond
%[text] ## Description
%[text] The method `createResult` resides in the class `lspsys`. It solves the system (via `solve2PortNetwork`) and returns a populated `result` object with the loudspeaker's frequency-domain response.

%[appendix]{"version":"1.0"}
%---
%[metadata:view]
%   data: {"layout":"inline"}
%---
